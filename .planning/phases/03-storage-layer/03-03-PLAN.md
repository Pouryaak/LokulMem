---
phase: 03-storage-layer
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/storage/MemoryRepository.ts
  - src/storage/index.ts
  - src/index.ts
autonomous: true
requirements:
  - STORAGE-02
must_haves:
  truths:
    - MemoryRepository provides CRUD operations for memories
    - All compound indexes are usable via repository methods
    - Repository uses embedding conversion for storage/retrieval
    - Storage layer is exported from public API
    - Type-safe access to all memory operations
  artifacts:
    - path: src/storage/MemoryRepository.ts
      provides: "Repository pattern for memory CRUD operations"
      min_lines: 200
      contains: "class MemoryRepository"
    - path: src/storage/index.ts
      provides: "Storage layer public exports"
      min_lines: 10
      contains: "export"
  key_links:
    - from: src/storage/MemoryRepository.ts
      to: src/storage/Database.ts
      via: "uses LokulDatabase"
      pattern: "LokulDatabase"
    - from: src/storage/MemoryRepository.ts
      to: src/storage/embeddingStorage.ts
      via: "uses memoryToDb/memoryFromDb"
      pattern: "memoryToDb|memoryFromDb"
    - from: src/index.ts
      to: src/storage/index.ts
      via: "re-exports storage types"
      pattern: "storage"
---

<objective>
Create MemoryRepository implementing the repository pattern for type-safe memory CRUD operations. This provides the primary interface for all memory storage operations and validates that all indexes work correctly.

Purpose: Provide a clean, type-safe API for memory persistence that downstream phases will use.
Output: MemoryRepository.ts with full CRUD operations using all defined indexes
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-storage-layer/03-CONTEXT.md
@.planning/phases/03-storage-layer/03-RESEARCH.md

@src/storage/Database.ts
@src/storage/embeddingStorage.ts
@src/internal/types.ts
@src/types/memory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MemoryRepository with CRUD operations</name>
  <files>src/storage/MemoryRepository.ts</files>
  <action>
    Create src/storage/MemoryRepository.ts:

    1. Imports:
       - LokulDatabase from './Database.js'
       - MemoryInternal from '../internal/types.js'
       - MemoryType, MemoryStatus from '../types/memory.js'
       - memoryToDb, memoryFromDb, DbMemoryRow from './embeddingStorage.js'

    2. Define MemoryFilter interface for query filtering:
       ```typescript
       interface MemoryFilter {
         types?: MemoryType[];
         status?: MemoryStatus;
         clusterId?: string;
         pinned?: boolean;
         minStrength?: number;
       }
       ```

    3. MemoryRepository class:
       - constructor(private db: LokulDatabase) {}

    4. Core CRUD methods:
       - async create(memory: MemoryInternal): Promise<void>
         - Convert to DbMemoryRow using memoryToDb
         - Use this.db.memories.add()

       - async getById(id: string): Promise<MemoryInternal | null>
         - Query by primary key
         - Convert back using memoryFromDb if found

       - async update(memory: MemoryInternal): Promise<void>
         - Convert to DbMemoryRow
         - Use this.db.memories.put() (upsert behavior)

       - async delete(id: string): Promise<void>
         - Use this.db.memories.delete()

    5. Query methods using indexes:
       - async findByTypeAndStatus(type: MemoryType, status: MemoryStatus): Promise<MemoryInternal[]>
         - Use compound index [types+status] with equals([type, status])

       - async findActiveByRecency(limit: number): Promise<MemoryInternal[]>
         - Use compound index [status+lastAccessedAt]
         - between(['active', 0], ['active', Date.now()])
         - reverse().limit(limit)

       - async findByCluster(clusterId: string, status?: MemoryStatus): Promise<MemoryInternal[]>
         - If status provided: use [clusterId+status] compound index
         - Else: use clusterId index

       - async findPinned(): Promise<MemoryInternal[]>
         - Use pinned index with equals(1) or equals(true)

       - async findByMentionCount(minCount: number, limit: number): Promise<MemoryInternal[]>
         - Use mentionCount index with above(minCount)
         - reverse().limit(limit)

       - async findByBaseStrengthRange(min: number, max: number, status?: MemoryStatus): Promise<MemoryInternal[]>
         - If status: use [status+baseStrength] compound index with between([status, min], [status, max])
         - Else: use baseStrength index with between(min, max)

    6. Bulk operations:
       - async bulkCreate(memories: MemoryInternal[]): Promise<void>
         - Map to DbMemoryRow[] using memoryToDb
         - Use this.db.memories.bulkAdd()

       - async bulkUpdate(memories: MemoryInternal[]): Promise<void>
         - Use this.db.memories.bulkPut()

    7. Count methods:
       - async count(filter?: MemoryFilter): Promise<number>
         - Apply filters and return count

    8. Utility methods:
       - async exists(id: string): Promise<boolean>
       - async getAllIds(): Promise<string[]>

    All query methods must convert DbMemoryRow results back to MemoryInternal using memoryFromDb.
    Handle null/undefined embeddings gracefully (should not happen but defensive coding).
  </action>
  <verify>npm run typecheck passes without errors</verify>
  <done>MemoryRepository.ts exists with all CRUD methods, query methods using all compound indexes, and bulk operations</done>
</task>

<task type="auto">
  <name>Task 2: Create storage layer index file</name>
  <files>src/storage/index.ts</files>
  <action>
    Create src/storage/index.ts as the public API for the storage layer:

    ```typescript
    /**
     * Storage layer exports
     * These are internal exports used by other parts of the library
     */

    // Database
    export { LokulDatabase } from './Database.js';

    // Storage Manager
    export { StorageManager } from './StorageManager.js';

    // Repository
    export { MemoryRepository } from './MemoryRepository.js';

    // Embedding utilities
    export {
      toDbFormat,
      fromDbFormat,
      memoryToDb,
      memoryFromDb,
      EXPECTED_EMBEDDING_DIM,
      type DbMemoryRow,
    } from './embeddingStorage.js';
    ```

    This provides a clean import surface for other modules.
  </action>
  <verify>npm run typecheck passes without errors</verify>
  <done>src/storage/index.ts exists with all storage layer exports</done>
</task>

<task type="auto">
  <name>Task 3: Add storage exports to main index</name>
  <files>src/index.ts</files>
  <action>
    Update src/index.ts to export storage-related types:

    1. Add to the existing export type block:
       ```typescript
       export type {
         // ... existing types ...
         // Storage types
         StorageStatus,
         StorageError,
         StorageErrorType,
       } from './types/index.js';
       ```

    2. Note: The actual storage classes (Database, StorageManager, MemoryRepository) are NOT exported publicly - they remain internal implementation details. Only the types are exported for users who need them for callbacks or configuration.

    Verify the exports are correct and don't expose internal implementation details.
  </action>
  <verify>npm run typecheck passes; npm run build succeeds</verify>
  <done>src/index.ts updated with StorageStatus, StorageError, StorageErrorType exports</done>
</task>

</tasks>

<verification>
- npm run typecheck passes with zero errors
- npm run build produces valid output
- MemoryRepository has methods using all compound indexes
- All CRUD operations are implemented
- Bulk operations use Dexie bulk methods
- Storage layer exports are clean and appropriate
</verification>

<success_criteria>
- MemoryRepository provides complete CRUD for memories
- All compound indexes are usable via repository methods
- Repository correctly handles Float32Array embedding conversion
- Storage layer is properly exported
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-storage-layer/03-03-SUMMARY.md`
</output>
