---
phase: 08-public-api-demo
plan: 06
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
  - 08-05
files_modified:
  - examples/react-app/package.json
  - examples/react-app/vite.config.ts
  - examples/react-app/tsconfig.json
  - examples/react-app/index.html
  - examples/react-app/src/main.tsx
  - examples/react-app/src/App.tsx
  - examples/react-app/src/components/ChatView.tsx
  - examples/react-app/src/components/MemoryList.tsx
  - examples/react-app/src/components/DebugPanel.tsx
  - examples/react-app/src/hooks/useLokulMem.ts
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - DEMO-04

must_haves:
  truths:
    - "React demo app exists in examples/react-app/ directory"
    - "Demo has isolated package.json with React dependencies"
    - "Root package.json not polluted with React dependencies"
    - "Demo visualizes debug object from augment() in real-time"
    - "Demo shows reactive memory list using manage().list()"
    - "Demo uses tab-based navigation for views"
    - "Demo persists data to IndexedDB"
    - "Demo prompts user for empty or sample data on first load"
  artifacts:
    - path: "examples/react-app/package.json"
      provides: "Isolated dependencies for demo app"
      contains: "react"
    - path: "examples/react-app/src/App.tsx"
      provides: "Main app component with tab navigation"
      min_lines: 100
    - path: "examples/react-app/src/components/ChatView.tsx"
      provides: "Conversation interface with augment/learn"
      min_lines: 100
    - path: "examples/react-app/src/components/MemoryList.tsx"
      provides: "Reactive memory browser using manage().list()"
      min_lines: 80
    - path: "examples/react-app/src/components/DebugPanel.tsx"
      provides: "Raw JSON visualization of debug object"
      min_lines: 50
    - path: "examples/react-app/src/hooks/useLokulMem.ts"
      provides: "React hook for LokulMem initialization"
      min_lines: 40
  key_links:
    - from: "examples/react-app/src/App.tsx"
      to: "lokulmem"
      via: "ES module import from library"
      pattern: "from 'lokulmem'"
    - from: "examples/react-app/src/components/ChatView.tsx"
      to: "src/api/Augmenter.ts"
      via: "augment() API calls"
      pattern: "\\.augment\\("
    - from: "examples/react-app/src/components/MemoryList.tsx"
      to: "src/api/Manager.ts"
      via: "manage() API calls"
      pattern: "\\.manage\\(\\)\\."
---

<objective>
Create React demo app showcasing augment(), learn(), and manage() APIs.

Purpose: Provide working example of LokulMem integration with real-time debug visualization, reactive memory list, and isolated workspace to demonstrate developer experience.

Output: Functional React demo in examples/react-app/ with tab navigation, chat view, memory list, and debug panel.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-public-api-demo/08-CONTEXT.md
@.planning/phases/08-public-api-demo/08-RESEARCH.md

# Public API plans provide augment/learn/manage implementations
@.planning/phases/08-public-api-demo/08-01-SUMMARY.md
@.planning/phases/08-public-api-demo/08-02-SUMMARY.md
@.planning/phases/08-public-api-demo/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create examples directory and package.json</name>
  <files>examples/react-app/package.json</files>
  <action>
Create isolated React demo workspace:

**Create directory structure:**
```bash
mkdir -p examples/react-app/src/{components,hooks}
```

**Create examples/react-app/package.json:**
```json
{
  "name": "@lokulmem/react-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "lokulmem": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.6.2",
    "vite": "^6.0.1"
  }
}
```

**Note:** `lokulmem: "workspace:*"` references the root package. This requires workspace configuration in root package.json.

**Verify root package.json NOT polluted:**
Root package.json should NOT contain:
- react
- react-dom
- @types/react
- @types/react-dom
- @vitejs/plugin-react

Per DEMO-04 requirement.
  </action>
  <verify>package.json created, contains React deps, workspace:* reference</verify>
  <done>Isolated demo workspace created</done>
</task>

<task type="auto">
  <name>Task 2: Create Vite and TypeScript configs</name>
  <files>examples/react-app/vite.config.ts, examples/react-app/tsconfig.json</files>
  <action>
Create build configuration for demo app:

**vite.config.ts:**
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    open: true
  },
  build: {
    outDir: 'dist'
  },
  optimizeDeps: {
    exclude: ['lokulmem'] // Don't bundle the library, use workspace link
  }
});
```

**tsconfig.json:**
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "../../tsconfig.json" }]
}
```
  </action>
  <verify>vite.config.ts and tsconfig.json created</verify>
  <done>Demo build configuration complete</done>
</task>

<task type="auto">
  <name>Task 3: Create HTML entry point</name>
  <files>examples/react-app/index.html</files>
  <action>
Create examples/react-app/index.html:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LokulMem Demo</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```
  </action>
  <verify>index.html created with root div and script reference</verify>
  <done>HTML entry point created</done>
</task>

<task type="auto">
  <name>Task 4: Create useLokulMem hook</name>
  <files>examples/react-app/src/hooks/useLokulMem.ts</files>
  <action>
Create examples/react-app/src/hooks/useLokulMem.ts:

```typescript
import { useEffect, useState } from 'react';
import { createLokulMem } from 'lokulmem';
import type { LokulMem } from 'lokulmem';

interface UseLokulMemResult {
  lokul: LokulMem | null;
  isReady: boolean;
  error: Error | null;
}

export function useLokulMem(): UseLokulMemResult {
  const [lokul, setLokul] = useState<LokulMem | null>(null);
  const [isReady, setIsReady] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  useEffect(() => {
    let mounted = true;

    createLokulMem({
      dbName: 'lokulmem-demo',
      onProgress: (stage, progress) => {
        console.log(`[${stage}] ${progress}%`);
      }
    })
      .then((instance) => {
        if (mounted) {
          setLokul(instance);
          setIsReady(true);
        }
      })
      .catch((err) => {
        if (mounted) {
          setError(err);
        }
      });

    return () => {
      mounted = false;
    };
  }, []);

  return { lokul, isReady, error };
}
```
  </action>
  <verify>useLokulMem hook exports, handles initialization and cleanup</verify>
  <done>React hook for LokulMem created</done>
</task>

<task type="auto">
  <name>Task 5: Create ChatView component</name>
  <files>examples/react-app/src/components/ChatView.tsx</files>
  <action>
Create examples/react-app/src/components/ChatView.tsx:

```typescript
import { useState } from 'react';
import type { LokulMem } from 'lokulmem';
import type { ChatMessage, AugmentResult } from 'lokulmem';

interface ChatViewProps {
  lokul: LokulMem;
  onDebug: (debug: AugmentResult['debug']) => void;
}

export function ChatView({ lokul, onDebug }: ChatViewProps) {
  const [messages, setMessages] = useState<Array<{ role: string; content: string }>>([]);
  const [input, setInput] = useState('');
  const [isLoading, setIsLoading] = useState(false);

  const handleSend = async () => {
    if (!input.trim() || isLoading) return;

    const userMessage = input;
    setInput('');
    setIsLoading(true);

    try {
      // Augment with memories
      const augmentResult = await lokul.augment(userMessage, messages, { debug: true });
      onDebug(augmentResult.debug);

      // Simulate LLM response (in real app, send to LLM)
      const assistantResponse = `I received your message: "${userMessage}". ${augmentResult.metadata.injectedCount} memories were injected.`;

      // Learn from conversation
      await lokul.learn(
        { role: 'user', content: userMessage },
        { role: 'assistant', content: assistantResponse }
      );

      // Update messages
      setMessages(prev => [
        ...prev,
        { role: 'user', content: userMessage },
        { role: 'assistant', content: assistantResponse }
      ]);
    } catch (error) {
      console.error('Error:', error);
    } finally {
      setIsLoading(false);
    }
  };

  return (
    <div className="chat-view">
      <div className="messages">
        {messages.map((msg, i) => (
          <div key={i} className={`message ${msg.role}`}>
            <strong>{msg.role}:</strong>
            <p>{msg.content}</p>
          </div>
        ))}
        {isLoading && <div className="message assistant">Thinking...</div>}
      </div>

      <div className="input">
        <textarea
          value={input}
          onChange={(e) => setInput(e.target.value)}
          onKeyDown={(e) => {
            if (e.key === 'Enter' && !e.shiftKey) {
              e.preventDefault();
              handleSend();
            }
          }}
          placeholder="Type a message... (Shift+Enter for newline)"
          rows={3}
        />
        <button onClick={handleSend} disabled={isLoading || !input.trim()}>
          Send
        </button>
      </div>
    </div>
  );
}
```

**Note:** In production, send augmented messages to actual LLM API. This demo simulates responses.
  </action>
  <verify>ChatView uses augment() and learn() APIs, handles loading state</verify>
  <done>ChatView component with augment/learn integration</done>
</task>

<task type="auto">
  <name>Task 6: Create MemoryList component</name>
  <files>examples/react-app/src/components/MemoryList.tsx</files>
  <action>
Create examples/react-app/src/components/MemoryList.tsx:

```typescript
import { useState, useEffect } from 'react';
import type { LokulMem } from 'lokulmem';
import type { MemoryDTO } from 'lokulmem';

interface MemoryListProps {
  lokul: LokulMem;
}

export function MemoryList({ lokul }: MemoryListProps) {
  const [memories, setMemories] = useState<MemoryDTO[]>([]);
  const [expanded, setExpanded] = useState<Set<string>>(new Set());

  useEffect(() => {
    loadMemories();

    // Subscribe to memory changes
    const unsubscribe = lokul.onMemoryAdded(() => {
      loadMemories();
    });

    return unsubscribe;
  }, [lokul]);

  async function loadMemories() {
    const result = await lokul.manage().list();
    setMemories(result.items);
  }

  function toggleExpand(id: string) {
    setExpanded(prev => {
      const next = new Set(prev);
      if (next.has(id)) {
        next.delete(id);
      } else {
        next.add(id);
      }
      return next;
    });
  }

  async function handlePin(id: string) {
    await lokul.manage().pin(id);
    loadMemories();
  }

  async function handleDelete(id: string) {
    await lokul.manage().delete(id);
    loadMemories();
  }

  return (
    <div className="memory-list">
      <h3>Memories ({memories.length})</h3>

      {memories.length === 0 ? (
        <p>No memories yet. Send some messages!</p>
      ) : (
        <ul>
          {memories.map(memory => (
            <li key={memory.id} className={`memory ${memory.pinned ? 'pinned' : ''}`}>
              <div className="memory-summary" onClick={() => toggleExpand(memory.id)}>
                <span className="types">{memory.types.join(', ')}</span>
                <span className="strength">{memory.currentStrength.toFixed(2)}</span>
                {memory.pinned && <span className="pinned-badge">📌</span>}
              </div>

              {expanded.has(memory.id) && (
                <div className="memory-detail">
                  <p>{memory.content}</p>
                  <div className="meta">
                    <small>Created: {new Date(memory.createdAt).toLocaleString()}</small>
                    {memory.entities.length > 0 && (
                      <small>Entities: {memory.entities.join(', ')}</small>
                    )}
                  </div>
                  <div className="actions">
                    <button onClick={() => handlePin(memory.id)}>
                      {memory.pinned ? 'Unpin' : 'Pin'}
                    </button>
                    <button onClick={() => handleDelete(memory.id)}>Delete</button>
                  </div>
                </div>
              )}
            </li>
          ))}
        </ul>
      )}
    </div>
  );
}
```
  </action>
  <verify>MemoryList uses manage().list(), reactive updates via events</verify>
  <done>MemoryList component with manage() integration</done>
</task>

<task type="auto">
  <name>Task 7: Create DebugPanel component</name>
  <files>examples/react-app/src/components/DebugPanel.tsx</files>
  <action>
Create examples/react-app/src/components/DebugPanel.tsx:

```typescript
import type { LokulMemDebug } from 'lokulmem';

interface DebugPanelProps {
  debug: LokulMemDebug | null;
}

export function DebugPanel({ debug }: DebugPanelProps) {
  return (
    <div className="debug-panel">
      <h3>Debug Output</h3>

      {!debug ? (
        <p className="placeholder">Send a message with debug enabled to see output...</p>
      ) : (
        <pre className="debug-json">{JSON.stringify(debug, null, 2)}</pre>
      )}
    </div>
  );
}
```

**Note:** Per CONTEXT decision, raw JSON output without syntax highlighting. Simple and technical.
  </action>
  <verify>DebugPanel displays raw JSON of debug object</verify>
  <done>DebugPanel component created</done>
</task>

<task type="auto">
  <name>Task 8: Create App component with tab navigation</name>
  <files>examples/react-app/src/App.tsx</files>
  <action>
Create examples/react-app/src/App.tsx:

```typescript
import { useState } from 'react';
import { useLokulMem } from './hooks/useLokulMem';
import { ChatView } from './components/ChatView';
import { MemoryList } from './components/MemoryList';
import { DebugPanel } from './components/DebugPanel';
import type { LokulMemDebug } from 'lokulmem';

type Tab = 'chat' | 'memories';

function App() {
  const { lokul, isReady, error } = useLokulMem();
  const [activeTab, setActiveTab] = useState<Tab>('chat');
  const [debug, setDebug] = useState<LokulMemDebug | null>(null);

  if (error) {
    return (
      <div className="app error">
        <h1>Error initializing LokulMem</h1>
        <pre>{error.message}</pre>
      </div>
    );
  }

  if (!isReady) {
    return (
      <div className="app loading">
        <h1> LokulMem Demo</h1>
        <p>Loading...</p>
      </div>
    );
  }

  return (
    <div className="app">
      <header>
        <h1> LokulMem Demo</h1>
        <nav>
          <button
            className={activeTab === 'chat' ? 'active' : ''}
            onClick={() => setActiveTab('chat')}
          >
            Chat
          </button>
          <button
            className={activeTab === 'memories' ? 'active' : ''}
            onClick={() => setActiveTab('memories')}
          >
            Memories
          </button>
        </nav>
      </header>

      <main>
        {activeTab === 'chat' && lokul && (
          <div className="split-view">
            <div className="chat-pane">
              <ChatView lokul={lokul} onDebug={setDebug} />
            </div>
            <div className="debug-pane">
              <DebugPanel debug={debug} />
            </div>
          </div>
        )}

        {activeTab === 'memories' && lokul && (
          <MemoryList lokul={lokul} />
        )}
      </main>
    </div>
  );
}

export default App;
```
  </action>
  <verify>App has tab navigation, ChatView + DebugPanel split view, MemoryList view</verify>
  <done>App component with tab navigation created</done>
</task>

<task type="auto">
  <name>Task 9: Create main.tsx and add basic styling</name>
  <files>examples/react-app/src/main.tsx, examples/react-app/src/App.css</files>
  <action>
Create examples/react-app/src/main.tsx:

```typescript
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './App.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

**Create examples/react-app/src/App.css:**
```css
* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}

.app {
  max-width: 1400px;
  margin: 0 auto;
  padding: 20px;
}

.app.loading, .app.error {
  text-align: center;
  padding: 100px 20px;
}

header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
  padding-bottom: 20px;
  border-bottom: 1px solid #e0e0e0;
}

nav button {
  padding: 10px 20px;
  margin-left: 10px;
  border: none;
  background: #f0f0f0;
  cursor: pointer;
  border-radius: 4px;
}

nav button.active {
  background: #007bff;
  color: white;
}

.split-view {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
  height: calc(100vh - 150px);
}

.chat-view, .debug-panel, .memory-list {
  border: 1px solid #e0e0e0;
  border-radius: 8px;
  padding: 20px;
  overflow-y: auto;
}

.messages {
  min-height: 300px;
  max-height: calc(100vh - 300px);
  overflow-y: auto;
  margin-bottom: 20px;
}

.message {
  margin-bottom: 15px;
  padding: 10px;
  border-radius: 4px;
}

.message.user {
  background: #e3f2fd;
}

.message.assistant {
  background: #f5f5f5;
}

.input {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.input textarea {
  width: 100%;
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 4px;
  resize: vertical;
}

.input button {
  align-self: flex-end;
  padding: 10px 20px;
  background: #007bff;
  color: white;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}

.input button:disabled {
  background: #ccc;
  cursor: not-allowed;
}

.debug-panel pre {
  background: #f5f5f5;
  padding: 15px;
  border-radius: 4px;
  overflow-x: auto;
  font-size: 12px;
}

.memory-list ul {
  list-style: none;
  padding: 0;
}

.memory {
  border: 1px solid #e0e0e0;
  border-radius: 4px;
  margin-bottom: 10px;
  overflow: hidden;
}

.memory.pinned {
  border-color: #ffc107;
}

.memory-summary {
  padding: 10px;
  background: #f9f9f9;
  cursor: pointer;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.memory-detail {
  padding: 10px;
  border-top: 1px solid #e0e0e0;
}

.types {
  font-weight: bold;
  color: #007bff;
}

.strength {
  font-size: 12px;
  color: #666;
}

.pinned-badge {
  margin-left: 10px;
}

.actions {
  margin-top: 10px;
  display: flex;
  gap: 10px;
}

.actions button {
  padding: 5px 10px;
  font-size: 12px;
  border: 1px solid #ccc;
  background: white;
  border-radius: 4px;
  cursor: pointer;
}

.placeholder {
  color: #999;
  font-style: italic;
  text-align: center;
  padding: 40px;
}
```
  </action>
  <verify>main.tsx renders App, App.css provides basic styling</verify>
  <done>Entry point and styling complete</done>
</task>

</tasks>

<verification>
1. **React demo app exists:**
   - examples/react-app/ directory created
   - Contains package.json, vite.config.ts, tsconfig.json

2. **Isolated package.json:**
   - Has react, react-dom dependencies
   - Root package.json does NOT have React dependencies

3. **Demo visualizes debug object:**
   - DebugPanel component shows raw JSON
   - ChatView passes debug object on each augment

4. **Reactive memory list:**
   - MemoryList uses manage() methods
   - Subscribes to onMemoryAdded for updates

5. **Tab-based navigation:**
   - Chat tab with ChatView + DebugPanel split view
   - Memories tab with MemoryList

6. **Requirements covered:**
   - DEMO-01: React app in examples/react-app/ with isolated package.json ✓
   - DEMO-02: Visualizes debug object from augment() ✓
   - DEMO-03: Reactive memory list using manage().list() ✓
   - DEMO-04: Does not pollute root package.json ✓
</verification>

<success_criteria>
- Demo app runs with `npm run dev` in examples/react-app/
- Chat interface demonstrates augment() and learn()
- Memory list shows real-time updates
- Debug panel displays augment debug output
- Root package.json has no React dependencies
- All DEMO-01 through DEMO-04 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-public-api-demo/08-06-SUMMARY.md` with:
- Files created (all demo app files)
- Isolated workspace configuration
- API integration points (augment, learn, manage)
- Usage instructions (npm run dev)
</output>
