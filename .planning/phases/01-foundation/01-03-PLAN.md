---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/types/index.ts
  - src/types/memory.ts
  - src/types/api.ts
  - src/types/events.ts
  - src/internal/types.ts
  - src/internal/dto.ts
  - tests/unit/types.test.ts
autonomous: true
requirements:
  - TS-01
  - TS-05
must_haves:
  truths:
    - "All public API types are defined and exported from src/types/"
    - "DTO pattern implemented: embeddings excluded from public interfaces"
    - "Internal types in src/internal/ are not exported publicly"
    - "Type tests verify DTO pattern excludes Float32Array from public API"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Public type re-exports"
      contains: ["export type", "MemoryDTO"]
    - path: "src/types/memory.ts"
      provides: "Memory-related public types"
      contains: ["interface MemoryDTO", "types: MemoryType[]"]
    - path: "src/types/api.ts"
      provides: "Public API interfaces"
      contains: ["interface LokulMemConfig", "interface AugmentOptions"]
    - path: "src/types/events.ts"
      provides: "Event callback types"
      contains: ["type MemoryCallback", "interface MemoryStats"]
    - path: "src/internal/types.ts"
      provides: "Internal types with embeddings"
      contains: ["interface MemoryInternal", "embedding: Float32Array"]
    - path: "src/internal/dto.ts"
      provides: "DTO conversion functions"
      contains: ["toMemoryDTO", "fromMemoryDTO"]
    - path: "src/index.ts"
      provides: "Updated with type re-exports"
      contains: ["export type"]
  key_links:
    - from: "src/types/memory.ts"
      to: "src/internal/types.ts"
      via: "DTO conversion functions"
      pattern: "toMemoryDTO|fromMemoryDTO"
    - from: "src/index.ts"
      to: "src/types/index.ts"
      via: "type re-exports"
      pattern: "export.*from.*types"
---

<objective>
Define core TypeScript types for the public API and internal implementation, implementing the DTO pattern to exclude embeddings from public interfaces.

Purpose: Establishes the type system that downstream phases will use — public DTOs for API consumers, internal types with embeddings for worker processing.
Output: Complete type definitions in src/types/ and src/internal/types.ts with DTO conversion utilities.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

## Implementation Notes

Per CONTEXT.md locked decisions:
- Types in dedicated src/types/ folder, organized by domain
- Internal-only modules in src/internal/ (not exported publicly)
- Named exports only
- No `any` type allowed at all
- DTO pattern: embeddings excluded from public API responses

Per REQUIREMENTS.md (Phase 1 - TS):
- TS-01: 100% TypeScript coverage for public API
- TS-05: DTO pattern: embeddings excluded from public API responses

Per RESEARCH.md patterns:
- MemoryInternal with Float32Array embedding (internal only)
- MemoryDTO without embedding (public)
- Conversion functions toMemoryDTO/toMemoryDTOs
- Explicit type imports (verbatimModuleSyntax compliance)

Type categories from REQUIREMENTS.md:
- Memory types: identity, location, profession, preference, project, temporal, relational, emotional
- Memory status: active, faded, archived, superseded
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public memory types with DTO pattern</name>
  <files>src/types/memory.ts, src/types/index.ts</files>
  <action>
Create src/types/memory.ts with public memory types (DTOs without embeddings):

1. MemoryType union type:
   - 'identity' | 'location' | 'profession' | 'preference' | 'project' | 'temporal' | 'relational' | 'emotional'

2. MemoryStatus union type:
   - 'active' | 'faded' | 'archived' | 'superseded'

3. MemoryDTO interface (public API):
   - id: string
   - content: string
   - types: MemoryType[] (memory can have multiple types per schema)
   - status: MemoryStatus
   - createdAt: number
   - updatedAt: number
   - validFrom: number
   - validTo: number | null
   - baseStrength: number
   - currentStrength: number
   - pinned: boolean
   - mentionCount: number
   - lastAccessedAt: number
   - clusterId: string | null
   - entities: string[]
   - sourceConversationIds: string[] (per spec: memories can belong to multiple conversations)
   - supersededBy: string | null
   - supersededAt: number | null
   - fadedAt: number | null
   - metadata: Record<string, unknown>
   - Note: NO embedding field (this is the DTO pattern)

4. Create src/types/index.ts that re-exports all public types:
   - export type { MemoryDTO, MemoryType, MemoryStatus } from './memory'

Use explicit type imports/exports per verbatimModuleSyntax.
  </action>
  <verify>cat src/types/memory.ts | grep -q 'interface MemoryDTO' && cat src/types/memory.ts | grep -q "types: MemoryType\[\]" && ! cat src/types/memory.ts | grep -q 'Float32Array' && cat src/types/index.ts | grep -q 'export type'</verify>
  <done>src/types/memory.ts exists with MemoryDTO (no embedding), MemoryType, MemoryStatus; src/types/index.ts re-exports public types</done>
</task>

<task type="auto">
  <name>Task 2: Create API and event types</name>
  <files>src/types/api.ts, src/types/events.ts, src/types/index.ts</files>
  <action>
Create API and event types:

1. src/types/api.ts:
   - LokulMemConfig interface:
     - dbName?: string
     - localModelBaseUrl?: string
     - workerUrl?: string
     - extractionThreshold?: number
     - onProgress?: (stage: InitStage, progress: number) => void
   - InitStage union type: 'worker' | 'model' | 'storage' | 'maintenance' | 'ready'
   - AugmentOptions interface:
     - maxTokens?: number
     - debug?: boolean
   - LearnOptions interface:
     - extractionThreshold?: number
   - LokulMemDebug interface (returned when debug: true):
     - injectedMemories: MemoryDTO[]
     - scores: Array<{ memoryId: string; relevance: number; breakdown: object }>
     - excludedCandidates: Array<{ memoryId: string; reason: string }>
     - tokenUsage: { prompt: number; completion: number; total: number }
     - latencyMs: number

2. src/types/events.ts:
   - MemoryStats interface:
     - totalMemories: number
     - activeMemories: number
     - fadedMemories: number
     - pinnedMemories: number
     - averageStrength: number
     - oldestMemoryAt: number | null
     - newestMemoryAt: number | null
   - Event callback types:
     - type MemoryCallback = (memory: MemoryDTO) => void
     - type MemoryIdCallback = (id: string) => void
     - type StatsCallback = (stats: MemoryStats) => void
     - type ContradictionCallback = (event: { oldMemory: MemoryDTO; newMemory: MemoryDTO; resolution: string }) => void
     - type Unsubscribe = () => void

3. Update src/types/index.ts to re-export all new types.

All types must be fully typed with no any.
  </action>
  <verify>cat src/types/api.ts | grep -q 'interface LokulMemConfig' && cat src/types/api.ts | grep -q 'type InitStage' && cat src/types/events.ts | grep -q 'interface MemoryStats' && cat src/types/events.ts | grep -q 'type MemoryCallback'</verify>
  <done>src/types/api.ts and src/types/events.ts exist with all API and event types; src/types/index.ts updated with re-exports</done>
</task>

<task type="auto">
  <name>Task 3: Create internal types with embeddings and DTO conversion</name>
  <files>src/internal/types.ts, src/index.ts</files>
  <action>
Create internal types and update main entry:

1. Create src/internal/types.ts:
   - MemoryInternal interface (extends MemoryDTO with embedding):
     - All fields from MemoryDTO PLUS
     - embedding: Float32Array (internal only, not in DTO)
   - EpisodeInternal interface (for future use):
     - id: string
     - summary: string
     - startMemoryId: string
     - endMemoryId: string
     - createdAt: number
   - EdgeInternal interface (for future use):
     - id: string
     - sourceMemoryId: string
     - targetMemoryId: string
     - similarity: number
     - createdAt: number

2. Create src/internal/dto.ts with conversion functions:
   - toMemoryDTO(internal: MemoryInternal): MemoryDTO
   - toMemoryDTOs(internals: MemoryInternal[]): MemoryDTO[]
   - fromMemoryDTO(dto: MemoryDTO, embedding: Float32Array): MemoryInternal
   - These functions explicitly handle the embedding field separation

3. Update src/index.ts:
   - Re-export all public types explicitly from './types/index.js'
   - Keep VERSION export
   - Keep WorkerUrl export
   - Do NOT export from './internal/' (internal types stay internal)
   - Use explicit type exports: export type { MemoryDTO, MemoryType, MemoryStatus, ... } from './types/index.js'

Verify no internal types are exported publicly.
  </action>
  <verify>cat src/internal/types.ts | grep -q 'interface MemoryInternal' && cat src/internal/types.ts | grep -q 'embedding: Float32Array' && cat src/index.ts | grep -q "export type" && ! cat src/index.ts | grep -q 'internal'</verify>
  <done>src/internal/types.ts exists with MemoryInternal (includes Float32Array), conversion functions exist, src/index.ts re-exports public types only</done>
</task>

</tasks>

<verification>
1. Run `npm run typecheck` to verify all types compile without errors
2. Run `npm run build` to verify types are generated in dist/
3. Verify dist/index.d.ts contains public types (no embedding field)
4. Verify src/internal/types.ts is NOT exported from src/index.ts
5. Run `npm run lint` to ensure no any types exist
</verification>

<success_criteria>
- src/types/memory.ts exists with MemoryDTO (no embedding field)
- src/types/api.ts exists with LokulMemConfig, AugmentOptions, LokulMemDebug
- src/types/events.ts exists with MemoryStats and callback types
- src/internal/types.ts exists with MemoryInternal (includes Float32Array)
- src/internal/dto.ts exists with toMemoryDTO/fromMemoryDTO conversion functions
- src/index.ts re-exports all public types from src/types/
- npm run typecheck passes with zero errors
- npm run build generates dist/index.d.ts with public types only
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
