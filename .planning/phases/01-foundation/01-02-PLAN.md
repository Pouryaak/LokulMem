---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - vitest.config.ts
  - src/index.ts
  - src/worker/index.ts
autonomous: true
requirements:
  - TS-02
  - TS-03
  - TS-04
must_haves:
  truths:
    - "Vite builds library with dual ESM/CJS output"
    - "Type declarations generated automatically via vite-plugin-dts"
    - "Worker code compiles as separate chunk"
    - "Build completes without errors"
  artifacts:
    - path: "vite.config.ts"
      provides: "Vite library mode configuration with worker support"
      contains: ["lib.entry", "worker.plugins", "dts plugin"]
    - path: "vitest.config.ts"
      provides: "Vitest test configuration"
      contains: ["environment: happy-dom", "globals: true"]
    - path: "src/index.ts"
      provides: "Main library entry point"
      contains: ["named exports"]
    - path: "src/worker/index.ts"
      provides: "Worker entry point"
      contains: ["self.onmessage"]
  key_links:
    - from: "vite.config.ts"
      to: "src/index.ts"
      via: "build.lib.entry.main"
      pattern: "entry.*main.*src/index.ts"
    - from: "vite.config.ts"
      to: "src/worker/index.ts"
      via: "build.lib.entry.worker"
      pattern: "entry.*worker.*src/worker/index.ts"
    - from: "src/index.ts"
      to: "src/worker/index.ts"
      via: "?worker&url import"
      pattern: "import.*\\?worker&url"
---

<objective>
Configure Vite for library mode with dual ESM/CJS output, automatic type declaration generation, and worker bundling as separate chunk.

Purpose: Establishes the build system that produces tree-shakeable bundles with proper TypeScript declarations and worker support.
Output: vite.config.ts, vitest.config.ts, and initial source entry points.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

## Implementation Notes

Per CONTEXT.md locked decisions:
- Worker import syntax: Use ?worker&url for bundler compatibility
- Worker code in src/worker/ folder with index.ts entry point
- Named exports only — no default export
- Source maps: Yes, separate files (not inline)
- Test environments: Unit tests in Node.js (happy-dom), integration in real browser (Playwright)

Per RESEARCH.md patterns:
- Use vite-plugin-dts with rollupTypes: true for single declaration file
- Configure worker.plugins separately from main plugins
- Use multiple entry points in build.lib.entry for main + worker
- Workers as ES modules with format: 'es'
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite configuration for library mode</name>
  <files>vite.config.ts</files>
  <action>
Create vite.config.ts with library mode configuration:

1. Import defineConfig from 'vite', resolve from 'path', dts from 'vite-plugin-dts'
2. Configure plugins:
   - dts({ include: ['src'], rollupTypes: true, insertTypesEntry: true })
3. Configure build.lib:
   - entry: { main: resolve(__dirname, 'src/index.ts'), worker: resolve(__dirname, 'src/worker/index.ts') }
   - name: 'LokulMem'
   - formats: ['es', 'cjs']
   - fileName: (format, entryName) => `${entryName}.${format === 'es' ? 'mjs' : 'cjs'}`
4. Configure build.rollupOptions:
   - external: [] (no deps in Phase 1; will add runtime deps like dexie, @xenova/transformers when imported)
   - output: { sourcemap: true, globals: {} }
5. Configure build options:
   - sourcemap: true
   - minify: 'esbuild'
   - target: 'es2020'
6. Configure worker:
   - format: 'es'
   - plugins: []

Use type imports where appropriate (verbatimModuleSyntax compliance).
  </action>
  <verify>cat vite.config.ts | grep -q 'defineConfig' && cat vite.config.ts | grep -q 'vite-plugin-dts' && cat vite.config.ts | grep -q 'worker:'</verify>
  <done>vite.config.ts exists with library mode, dual entry points (main + worker), dts plugin, and worker configuration</done>
</task>

<task type="auto">
  <name>Task 2: Create Vitest configuration file</name>
  <files>vitest.config.ts</files>
  <action>
Create vitest.config.ts for test configuration:

1. Import defineConfig from 'vitest/config'
2. Export default defineConfig with:
   - test.environment: 'happy-dom' (for DOM mocking in Node.js)
   - test.globals: true (enable global test APIs)
   - test.include: ['tests/unit/**/*.test.ts']
   - test.exclude: ['node_modules', 'dist', 'examples']

This provides explicit Vitest configuration separate from Vite's inline config.
  </action>
  <verify>cat vitest.config.ts | grep -q 'environment.*happy-dom' && cat vitest.config.ts | grep -q 'globals.*true'</verify>
  <done>vitest.config.ts exists with happy-dom environment and globals enabled</done>
</task>

<task type="auto">
  <name>Task 3: Create main library entry point and worker entry</name>
  <files>src/index.ts, src/worker/index.ts</files>
  <action>
Create initial source files:

1. src/index.ts (main entry):
   - Add header comment: "LokulMem - Browser-Native LLM Memory Management Library"
   - Export a version constant: export const VERSION = '0.1.0'
   - Export placeholder types that will be expanded in later phases
   - Use named exports only (per user decision)
   - Import worker URL pattern: import WorkerUrl from './worker/index.ts?worker&url'
   - Export the WorkerUrl for advanced use cases

2. src/worker/index.ts (worker entry):
   - Add header comment: "LokulMem Worker - Runs in Web Worker context"
   - Set up basic worker message handler:
     ```typescript
     self.onmessage = (event: MessageEvent) => {
       const { type, payload } = event.data
       // Echo back for now - will implement in Phase 2
       self.postMessage({ type: `${type}:response`, payload })
     }
     ```
   - Export empty object to make it a module

Both files should pass strict TypeScript checking with no any types.
  </action>
  <verify>test -f src/index.ts && test -f src/worker/index.ts && cat src/index.ts | grep -q 'VERSION' && cat src/worker/index.ts | grep -q 'self.onmessage'</verify>
  <done>src/index.ts and src/worker/index.ts exist with proper structure, named exports, and worker message handler</done>
</task>

</tasks>

<verification>
1. Run `npm install` to install dependencies
2. Run `npm run build` to verify Vite configuration works
3. Verify dist/ folder contains: main.mjs, main.cjs, worker.mjs, worker.cjs, index.d.ts
4. Verify source maps are generated (.map files)
5. Run `npm run typecheck` to verify TypeScript compiles without errors
</verification>

<success_criteria>
- vite.config.ts exists with library mode, dual entry points, and dts plugin
- vitest.config.ts exists with happy-dom environment
- src/index.ts exists with named exports and worker URL import
- src/worker/index.ts exists with message handler
- npm run build produces dist/ with main.mjs/cjs, worker.mjs/cjs, and .d.ts files
- npm run typecheck passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
