---
phase: 02-worker-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/core/WorkerManager.ts
  - src/core/LokulMem.ts
  - src/index.ts
autonomous: true
requirements:
  - WORKER-05
must_haves:
  truths:
    - LokulMem class integrates WorkerManager with progress callbacks
    - onProgress callback receives (stage, progress) during initialization
    - Library exposes a clean factory function or class for instantiation
    - Worker initialization is awaitable and resolves when 'ready' stage reached
    - Public API types are re-exported from src/index.ts
  artifacts:
    - path: src/core/LokulMem.ts
      provides: Main LokulMem class with initialization and progress
      exports: [LokulMem, createLokulMem]
    - path: src/core/WorkerManager.ts
      provides: Updated to integrate with WorkerClient
      exports: [WorkerManager]
    - path: src/index.ts
      provides: Public API exports
      exports: [createLokulMem, VERSION, all public types]
  key_links:
    - from: src/core/LokulMem.ts
      to: src/core/WorkerManager.ts
      via: WorkerManager for worker initialization
      pattern: "import.*WorkerManager"
    - from: src/core/LokulMem.ts
      to: src/core/MessagePort.ts
      via: WorkerClient for communication
      pattern: "import.*WorkerClient"
    - from: src/index.ts
      to: src/core/LokulMem.ts
      via: re-export createLokulMem
      pattern: "export.*createLokulMem"
---

<objective>
Integrate WorkerManager with WorkerClient to create the LokulMem class that handles initialization with progress reporting and provides the foundation for the public API.

Purpose: This is the main entry point that users will interact with. It orchestrates worker initialization, progress reporting, and provides the base for augment/learn/manage APIs in later phases.

Output: LokulMem class with initialize() method, updated WorkerManager integration, and clean public API exports.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/skills/typescript/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-worker-infrastructure/02-CONTEXT.md
@.planning/phases/02-worker-infrastructure/02-RESEARCH.md
@src/types/api.ts
@src/core/WorkerManager.ts
@src/core/MessagePort.ts
@src/core/Protocol.ts
@src/worker/index.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update WorkerManager to use WorkerClient</name>
  <files>src/core/WorkerManager.ts</files>
  <action>
    Update src/core/WorkerManager.ts to integrate with WorkerClient:

    1. Import WorkerClient and createMainThreadPort from ./MessagePort.js
    2. Import message types from ./Protocol.ts

    3. Add to WorkerManager class:
       - private client: WorkerClient | null = null
       - private onProgressCallback: ((stage: InitStage, progress: number) => void) | null = null

    4. Update initialize() method:
       - Accept optional onProgress callback in config
       - After getting port, create WorkerClient(port)
       - Set up client.onMessage to handle ProgressMessage
       - When progress message received: call onProgress callback with (stage, overallProgress)
       - Send INIT request via client.request()
       - Wait for init completion
       - Return this for chaining

    5. Add method getClient(): WorkerClient | null

    6. Update createMainThreadPort():
       - Use createMainThreadPort from MessagePort.ts
       - Set up message routing to in-memory worker handlers

    7. Update terminate():
       - Call client.terminate() if exists
       - Clean up worker

    Per TypeScript skill: use async/await, proper error handling with console.error.
  </action>
  <verify>grep -q "WorkerClient" src/core/WorkerManager.ts && grep -q "onProgress" src/core/WorkerManager.ts && grep -q "client.request" src/core/WorkerManager.ts</verify>
  <done>WorkerManager integrates with WorkerClient and handles progress messages</done>
</task>

<task type="auto">
  <name>Task 2: Create LokulMem main class</name>
  <files>src/core/LokulMem.ts</files>
  <action>
    Create src/core/LokulMem.ts - the main class users interact with:

    1. Import types:
       - LokulMemConfig, InitStage from ../types/api.js
       - WorkerManager from ./WorkerManager.js
       - WorkerClient from ./MessagePort.js

    2. Export class LokulMem:
       - private workerManager: WorkerManager
       - private config: Required<LokulMemConfig>
       - private isInitialized = false

    3. Constructor(config: LokulMemConfig = {}):
       - Set defaults for all config options:
         - dbName: 'lokulmem-default'
         - workerType: 'auto'
         - initTimeoutMs: 10000
         - maxRetries: 3
       - Merge with provided config
       - Create WorkerManager instance

    4. Method async initialize(): Promise<void>
       - If already initialized, return immediately
       - Call workerManager.initialize() with:
         - workerUrl: imported from '../worker/index.ts?worker&url'
         - onProgress callback from config
       - Set isInitialized = true on success
       - Throw on failure

    5. Method getWorkerType(): 'shared' | 'dedicated' | 'main'
       - Return workerManager.getWorkerType()

    6. Method getPersistenceStatus(): PersistenceStatus | null
       - Return workerManager.getPersistenceStatus()

    7. Method isReady(): boolean
       - Return isInitialized

    8. Method terminate(): void
       - Call workerManager.terminate()
       - Set isInitialized = false

    9. Export async function createLokulMem(config?: LokulMemConfig): Promise<LokulMem>
       - Create LokulMem instance
       - Call initialize()
       - Return instance
       - This is the main factory function users will call

    Per TypeScript skill: use interface for config, explicit return types on public methods.
  </action>
  <verify>grep -q "export class LokulMem" src/core/LokulMem.ts && grep -q "export async function createLokulMem" src/core/LokulMem.ts && grep -q "initialize" src/core/LokulMem.ts</verify>
  <done>LokulMem class exists with initialize(), getWorkerType(), createLokulMem factory</done>
</task>

<task type="auto">
  <name>Task 3: Update public API exports</name>
  <files>src/index.ts</files>
  <action>
    Update src/index.ts to export the complete public API:

    1. Keep existing type exports from src/types/index.js:
       - MemoryDTO, MemoryType, MemoryStatus
       - LokulMemConfig, InitStage, AugmentOptions, LearnOptions, LokulMemDebug
       - MemoryStats, MemoryCallback, MemoryIdCallback, StatsCallback, ContradictionCallback, Unsubscribe

    2. Add new exports:
       - Export LokulMem class and createLokulMem factory from ./core/LokulMem.js
       - Export VERSION (keep existing)
       - Export WorkerUrl (keep existing)

    3. Ensure internal types are NOT exported:
       - MemoryInternal, EpisodeInternal, EdgeInternal stay internal
       - WorkerClient, WorkerManager stay internal
       - Protocol types stay internal

    4. The public API surface should be:
       - createLokulMem(config?) - factory function
       - LokulMem class (for advanced users)
       - VERSION constant
       - WorkerUrl (for custom worker instantiation)
       - All public types from src/types/

    Per TypeScript skill: use named exports only (already established pattern).
  </action>
  <verify>grep -q "export.*createLokulMem" src/index.ts && grep -q "export.*LokulMem" src/index.ts && grep -q "export.*VERSION" src/index.ts</verify>
  <done>Public API exports createLokulMem, LokulMem, VERSION, WorkerUrl, and all public types</done>
</task>

</tasks>

<verification>
- LokulMem class can be instantiated and initialized
- Progress callback receives correct stage and progress values
- Worker type is accessible after initialization
- Factory function createLokulMem works correctly
- All public types are exported
</verification>

<success_criteria>
1. src/core/LokulMem.ts exists with LokulMem class and createLokulMem factory
2. WorkerManager is updated to use WorkerClient and handle progress
3. src/index.ts exports clean public API
4. onProgress callback works through all 5 init stages
5. Library can be initialized with createLokulMem(config)
6. Worker type is exposed via getWorkerType()
</success_criteria>

<output>
After completion, create `.planning/phases/02-worker-infrastructure/02-03-SUMMARY.md`
</output>
