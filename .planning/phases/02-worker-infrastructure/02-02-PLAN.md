---
phase: 02-worker-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/Protocol.ts
  - src/core/MessagePort.ts
  - src/worker/index.ts
autonomous: true
requirements:
  - WORKER-05
must_haves:
  truths:
    - Message protocol supports request/response correlation with unique IDs
    - Progress messages include stage, stageProgress, and overallProgress
    - Worker reports all 5 init stages: worker, model, storage, maintenance, ready
    - MessagePort abstraction works for SharedWorker, DedicatedWorker, and main thread
    - Protocol types are shared between main thread and worker
  artifacts:
    - path: src/core/Protocol.ts
      provides: Message protocol types and validation
      exports: [RequestMessage, ResponseMessage, ProgressMessage, MessageType]
    - path: src/core/MessagePort.ts
      provides: MessagePort abstraction for all worker types
      exports: [WorkerClient, createMainThreadPort]
    - path: src/worker/index.ts
      provides: Worker entry point with progress reporting
      exports: [setupPort, reportProgress]
  key_links:
    - from: src/core/MessagePort.ts
      to: src/core/Protocol.ts
      via: import message types
      pattern: "import.*from.*Protocol"
    - from: src/worker/index.ts
      to: src/core/Protocol.ts
      via: import message types
      pattern: "import.*from.*core/Protocol"
    - from: src/core/WorkerManager.ts
      to: src/core/MessagePort.ts
      via: WorkerClient for communication
      pattern: "import.*WorkerClient"
---

<objective>
Create the message protocol and communication layer that enables progress reporting through 5 stages (worker, model, storage, maintenance, ready) with request/response correlation.

Purpose: The worker and main thread need a structured way to communicate. Progress reporting (WORKER-05) requires specific message formats that both sides understand.

Output: Protocol.ts with message types, MessagePort.ts with WorkerClient for request/response handling, and updated worker/index.ts with progress reporting.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/skills/typescript/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-worker-infrastructure/02-CONTEXT.md
@.planning/phases/02-worker-infrastructure/02-RESEARCH.md
@src/types/api.ts
@src/worker/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create message protocol types</name>
  <files>src/core/Protocol.ts</files>
  <action>
    Create src/core/Protocol.ts with message protocol types:

    1. Import InitStage from ../types/api.js

    2. Export interface RequestMessage:
       - id: string (UUID for correlation)
       - type: string
       - payload: unknown

    3. Export interface ResponseMessage:
       - id: string (matches request)
       - type: string
       - payload: unknown
       - error?: { code: string; message: string; details?: unknown }

    4. Export interface ProgressMessage:
       - type: 'progress'
       - stage: InitStage
       - stageProgress: number (0-100 within stage)
       - overallProgress: number (0-100 across all stages)
       - message?: string

    5. Export type WorkerMessage = RequestMessage | ResponseMessage | ProgressMessage

    6. Export const enum MessageType with values:
       - INIT = 'init'
       - PROGRESS = 'progress'
       - PING = 'ping'
       - PONG = 'pong'
       - ERROR = 'error'

    7. Export interface InitPayload:
       - dbName: string
       - persistenceGranted: boolean
       - modelConfig?: unknown

    Per TypeScript skill: use interface for object shapes, type for unions.
  </action>
  <verify>grep -q "export interface RequestMessage" src/core/Protocol.ts && grep -q "export interface ProgressMessage" src/core/Protocol.ts && grep -q "export type WorkerMessage" src/core/Protocol.ts</verify>
  <done>All message types defined and exported with correct fields</done>
</task>

<task type="auto">
  <name>Task 2: Create WorkerClient for request/response handling</name>
  <files>src/core/MessagePort.ts</files>
  <action>
    Create src/core/MessagePort.ts with WorkerClient class:

    1. Import types from ./Protocol.ts

    2. Export class WorkerClient:
       - private port: MessagePort
       - private pendingRequests: Map<string, { resolve, reject, timeout }>
       - private messageHandler: ((event: MessageEvent) => void) | null = null

    3. Constructor(port: MessagePort):
       - Store port
       - Bind handleMessage and handleMessageError
       - Set up port.onmessage and port.onmessageerror

    4. Method request(type: string, payload: unknown, timeoutMs = 5000): Promise<unknown>
       - Generate id using crypto.randomUUID() or fallback
       - Return Promise that:
         - Sets timeout to reject after timeoutMs
         - Stores pending request in Map
         - Posts message with { id, type, payload }
       - On timeout: remove from Map, reject with error

    5. Method onMessage(handler: (message: WorkerMessage) => void): void
       - Set messageHandler for non-response messages (like progress)

    6. Method terminate(): void
       - Clear all pending requests (reject with 'terminated')
       - Remove event listeners
       - Close port if possible

    7. Private handleMessage(event: MessageEvent<ResponseMessage | ProgressMessage>):
       - If message has id in pendingRequests: resolve/reject and cleanup
       - Else if messageHandler exists: call with message

    8. Private handleMessageError(event: MessageEvent):
       - console.error('Message deserialization error:', event)

    9. Export function createMainThreadPort(): MessagePort
       - Returns a MessagePort-like object for main thread fallback
       - This creates a pair of MessagePorts using MessageChannel
       - One port is returned, the other is used for "worker" side
       - The "worker" side will route to in-memory handlers

    Per RESEARCH.md patterns:
    - Use request/response with UUID correlation
    - Handle messageerror events
    - Clean up timeouts to prevent memory leaks
  </action>
  <verify>grep -q "class WorkerClient" src/core/MessagePort.ts && grep -q "request.*Promise" src/core/MessagePort.ts && grep -q "createMainThreadPort" src/core/MessagePort.ts</verify>
  <done>WorkerClient class exists with request/response correlation, timeout handling, and main thread port creation</done>
</task>

<task type="auto">
  <name>Task 3: Update worker entry point with progress reporting</name>
  <files>src/worker/index.ts</files>
  <action>
    Rewrite src/worker/index.ts to be a proper worker entry point with progress reporting:

    1. Import types from ../core/Protocol.ts:
       - RequestMessage, ResponseMessage, ProgressMessage, MessageType, InitPayload

    2. Import InitStage from ../types/api.js

    3. Define stage weights for progress calculation:
       - worker: 0.2, model: 0.2, storage: 0.2, maintenance: 0.2, ready: 0.2

    4. Function calculateOverallProgress(stage: InitStage, stageProgress: number): number
       - Calculate based on stage order and weights

    5. Function reportProgress(port: MessagePort, stage: InitStage, stageProgress: number):
       - Calculate overallProgress
       - Post ProgressMessage to port

    6. Function setupPort(port: MessagePort):
       - Set port.onmessage handler
       - Handle MessageType.INIT: call handleInit
       - Handle MessageType.PING: respond with PONG
       - Handle unknown types: log but ignore

    7. Async function handleInit(port: MessagePort, payload: InitPayload):
       - Report progress: worker stage 100%, overall 20%
       - (Stub) Report progress: model stage 0-100%, overall 20-40%
       - (Stub) Report progress: storage stage 0-100%, overall 40-60%
       - (Stub) Report progress: maintenance stage 0-100%, overall 60-80%
       - Report progress: ready stage 100%, overall 100%
       - Send init:complete response

    8. SharedWorker entry point:
       - onconnect = (event) => { setupPort(event.ports[0]); event.ports[0].start(); }

    9. DedicatedWorker entry point:
       - if (typeof onconnect === 'undefined') { setupPort(self as unknown as MessagePort); }

    10. Add stubs for future phases:
        - initializeModel(config): Promise<void>
        - initializeStorage(dbName): Promise<void>
        - runMaintenance(): Promise<void>

    Per RESEARCH.md patterns:
    - Use onconnect for SharedWorker
    - Check typeof onconnect for DedicatedWorker detection
    - Call port.start() for SharedWorker ports
    - Report progress through all 5 stages
  </action>
  <verify>grep -q "function reportProgress" src/worker/index.ts && grep -q "function setupPort" src/worker/index.ts && grep -q "onconnect" src/worker/index.ts && grep -q "handleInit" src/worker/index.ts</verify>
  <done>Worker entry point has setupPort, reportProgress, onconnect handler, and reports all 5 init stages</done>
</task>

</tasks>

<verification>
- Protocol types support request/response correlation
- Progress messages include stage and both progress values
- WorkerClient handles timeouts and cleanup
- Worker reports all 5 stages in correct order
</verification>

<success_criteria>
1. src/core/Protocol.ts exists with all message types
2. src/core/MessagePort.ts exists with WorkerClient class
3. src/worker/index.ts is rewritten with progress reporting
4. Request/response correlation uses UUID-based IDs
5. Progress reporting calculates overall progress correctly
6. Worker handles both SharedWorker and DedicatedWorker entry points
</success_criteria>

<output>
After completion, create `.planning/phases/02-worker-infrastructure/02-02-SUMMARY.md`
</output>
